{-# OPTIONS --safe #-}

-- This module is not allowed to import any files from the
-- `Categories.APROP` subtree!

module Categories.APROP.Hypergraph.CompletenessAssumptions where

record Assumptions : Set where
